apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: notification-service
spec:
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: metrics
      port: 9090
      targetPort: 9090
